package managers;

public class LevelState {
    // not going to add anything to this yet as it may be un-needed
    // essentially, we need to conver the entire level into a state so we can save
    // it to a txt file or whatever.
    // supposedly, this class would do that.
}
